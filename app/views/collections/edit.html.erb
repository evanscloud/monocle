<div class="custform">
  <h1>Edit <%= @collection.name %> Collection</h1>

  <div class="form-input">
    <%= form_for @collection, :url => collection_path(@collection.id) do |f| %>
    <%= render "collection_errors" %>

      <div class="field">
        <%= f.label :name %><br>
        <%= f.text_field :name, class: "form-control", autofocus: true %>
      </div>

      <div class="actions">
        <%= f.submit "Update", class: "btn btn-default" %>
      </div>

    <% end %>
  </div>
</div>

<div class="custbtn-container">
  <h3><%= button_to "Back", user_collections_path(@collection.user_id), method: 'get', class: "btn btn-default" %></h3>
</div>
