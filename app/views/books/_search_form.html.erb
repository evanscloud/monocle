<%= render "book_errors" %>

<div class="form-input">
  <%= form_for @book, :url => search_path do |f| %>

    <div class="field">
      <%= f.text_field :title, class: "form-control", autofocus: true %>
    </div>

    <div class="actions">
      <%= f.submit "Search Book", class: "btn btn-default" %>
    </div>
  <% end %>
</div>
